#include "HekUIManager.hpp"

namespace hekui {



    // HekUIManager::HekUIManager(const GLFWwindow* window) {
    //     windowHandle_ = window;
    //     // projMat_ = glm::ortho(0.0f, (float)window_->getWidth(),
    //         // (float)window_->getHeight(), 0.0f, 0.0f, (float)maxLayers_);
    // }
}